/**
 * Scoring weights and constants
 * Used throughout the app for calculating combined scores
 */

export const SCORE_WEIGHTS = {
  ASSESSMENT: 0.7,
  TECH_STACK: 0.3,
} as const;

export const MAX_SCORE = 100;
export const MIN_SCORE = 0;

